/**[@test({ "title": "TruJS.compile.type.module._ModuleFileFinder: " })]*/
function testModuleFileFinder(arrange, act, assert, module) {
    var moduleFileFinder;

    arrange(function () {

    });

    act(function () {

    });

    assert(function (test) {

    });
}