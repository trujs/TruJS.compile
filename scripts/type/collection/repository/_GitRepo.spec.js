/**[@test({ "title": "TruJS.compile.type.collection.repository._GitRepo: " })]*/
function testGetRepo(arrange, assert, act, module) {
    var getRepo;

    arrange(function () {
        getRepo = module(["TruJS.compile.type.collection.repository._GitRepo", []]);
    });

    act(function () {

    });

    assert(function (test) {

    });
}