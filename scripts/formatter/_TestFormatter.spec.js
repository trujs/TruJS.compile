/**[@test({ "title": "TruJS.compile.formatter._TestFormatter: " })]*/
function testTestFormatter(arrange, act, assert, module) {
    var testFormatter;

    arrange(function () {

    });

    act(function () {

    });

    assert(function (test) {

    });
}